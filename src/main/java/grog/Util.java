package grog;

public class Util {

    /*public static SymbolsTable symbolsTableWithLambdaParameters(SymbolsTable parent, Lambda lambda, GrogBaseListener listener) {
        var parameters = lambda.parameters();
        var newSymbolsTable = new SymbolsTable(parent);
        for (int i = 0; i < parameters.size(); i++) {
                newSymbolsTable.put(parameters.get(i).name(), ctx.parameters.get(i).accept(listener));
        }
    }*/
    
}
